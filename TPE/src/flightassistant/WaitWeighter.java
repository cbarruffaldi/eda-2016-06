package flightassistant;

public class WaitWeighter {
	
	@Override
	public WeightedFlight minFlight(Airport from, Airport to) {
		A
		Flight shortest = from.getShortestTo(to);
		Airport
		return new WeightedFlight(quickest, );
	}
	
}
